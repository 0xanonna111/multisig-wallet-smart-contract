# multisig-wallet-smart-contract
A secure Multi-Signature (Multisig) wallet implementation. This contract requires a predefined number of approvals (M-of-N) from designated owners before any transaction (sending ETH or calling other contracts) can be executed.
